[package]
name = "wasm-gr-diff-compressor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
lz4_flex = { version = "0.11", default-features = false, features = ["std"] }
serde_json = "1.0"
log = "0.4"
